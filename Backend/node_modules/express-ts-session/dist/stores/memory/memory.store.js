"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MemoryStore = void 0;
class MemoryStore {
    constructor() {
        this.sessions = {};
    }
    get(sid) {
        const existingSession = this.sessions[sid];
        if (!existingSession)
            throw new Error("There is no existing session in the store but a cookie was sent with a session id");
        return existingSession;
    }
    set(sid, data) {
        this.sessions[sid] = data;
    }
    destroy(sid) {
        delete this.sessions[sid];
    }
    clear() {
        this.sessions = Object.create({});
    }
}
exports.MemoryStore = MemoryStore;
